/**
 * Minimal `Service` base stub for the test bundle: AdvisorService extends it,
 * but the tests supply their own host context and never use cordis DI.
 */
export class Service {
  constructor(_ctx: unknown, _name: string) {}
}
